# Copia este archivo a .env y rellena los valores

# ID de tu proyecto en Google Cloud
GOOGLE_CLOUD_PROJECT=tu-proyecto-id

# Región de Vertex AI (us-central1 tiene soporte para Imagen 3)
VERTEX_LOCATION=us-central1

# Ruta al JSON de tu Service Account (con rol "Vertex AI User")
# Descárgalo en: IAM & Admin > Service Accounts > tu-cuenta > Keys > Add Key
GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json

# ── DALL-E 3 (OpenAI) ─────────────────────────────────────
# Obtén tu clave en: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Puerto del servidor proxy (opcional, default 3001)
# PORT=3001
